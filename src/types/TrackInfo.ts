export type TrackInfo = {
    id: number,
    name: string,
    author: string,
    release_date: Date,
    genre: string,
    duration_in_seconds: number,
    album: string,
    track_file: string
};